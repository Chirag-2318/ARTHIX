<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Phase 2.1: Bank SMS reading -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <!-- Phase 4: Camera OCR (FR-4) and Voice STT (FR-3) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name=".ArthixApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

        <!-- MAIN / LAUNCHER ACTIVITY -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Shake detection foreground service -->
        <service
            android:name=".sensor.ShakeDetectionService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

        <!-- Bounded background capture grace window service -->
        <service
            android:name=".sensor.CaptureGraceWindowService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

        <!-- Floating Pop-up Window Service (Shake-to-Log Overlay) -->
        <service
            android:name=".ui.overlay.FloatingChipOverlayService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

        <!-- Phase 2: UPI notification listener (PRD §2.1) -->
        <service
            android:name=".notification.UpiNotificationListenerService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Phase 2.1: Bank SMS receiver -->
        <receiver
            android:name=".sms.BankSmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="0">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Phase 3: Chip category-selection tap handler -->
        <receiver
            android:name=".ui.chip.ChipActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.chirag.arthix.action.CATEGORY_SELECTED" />
            </intent-filter>
        </receiver>

        <!-- Phase 4: Receipt camera OCR (FR-4) -->
        <activity
            android:name=".ocr.ReceiptCaptureActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

    </application>

</manifest>