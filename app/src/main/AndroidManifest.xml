<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Phase 2.1: Bank SMS reading -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:name=".ArthixApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

        <!-- MAIN / LAUNCHER ACTIVITY -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Shake detection foreground service -->
        <service
            android:name=".sensor.ShakeDetectionService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

        <!-- Phase 2: UPI notification listener (PRD §2.1) -->
        <service
            android:name=".notification.UpiNotificationListenerService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Phase 2.1: Bank SMS receiver -->
        <receiver
            android:name=".sms.BankSmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="0">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Phase 3: Chip category-selection tap handler -->
        <receiver
            android:name=".ui.chip.ChipActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.chirag.arthix.action.CATEGORY_SELECTED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>